{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt && cp main2.py /app/bot.py"
  },
  "deploy": {
    "startCommand": "python /app/bot.py",
    "variables": {
      "BOT_TOKEN": {
        "description": "Токен из @BotFather",
        "required": true
      },
      "DATABASE_PATH": {
        "value": "/data/database.db",
        "description": "Путь к БД"
      }
    }
  }
}
